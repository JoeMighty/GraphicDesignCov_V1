# Public Supabase project URL and publishable/anon key (safe to expose to the browser)
NEXT_PUBLIC_SUPABASE_URL=https://dfvenjfmtokcqhtkryoo.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_-sWVz0pfwSTH5NJATAeXDQ_oaODBV_B

# Secret — Supabase project settings > API > service_role key. Server-only, never expose to the client.
SUPABASE_SERVICE_ROLE_KEY=

# Secret — shared password staff use to log into /admin
ADMIN_PASSWORD=

# Secret — random string used to sign the admin session cookie (e.g. `openssl rand -hex 32`)
SESSION_SECRET=
